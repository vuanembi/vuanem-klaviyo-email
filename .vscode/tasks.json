{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "npx mailwind --input-html src/${fileBasename} --output-html build/${fileBasename}",
            "problemMatcher": []
        }
    ]
}
